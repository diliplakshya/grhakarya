apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  namespace: {{ printf "%s-%s" .Values.namespace .Values.global.env }}
type: Opaque
stringData:
{{ printf "environments/%s/secret.yaml" .Values.global.env | .Files.Get | indent 2 }}