apiVersion: v1
kind: Service
metadata:
  name: {{ include "grhakarya.fullname" . }}
  labels:
    {{- include "grhakarya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grhakarya.selectorLabels" . | nindent 4 }}
