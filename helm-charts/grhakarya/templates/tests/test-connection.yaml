apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grhakarya.fullname" . }}-test-connection"
  labels:
    {{- include "grhakarya.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grhakarya.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
